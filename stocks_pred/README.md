# Stock Prediction

A linear regression model that predicts if an `open` value of a stock will be higher than the `close` value, by reading a ticker from `Yahoo Finance` API.

<img src="https://github.com/danvargg/folio/blob/master/stocks_pred/stocks.PNG" width=50%>